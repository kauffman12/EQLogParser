<local:BreakdownTable x:Class="EQLogParser.HealBreakdown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:local="clr-namespace:EQLogParser"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.Resources>
      <local:ZeroConverter x:Key="ZeroConverter" />
      <local:NumberFormatConverter x:Key="NumberFormatConverter" />
    </Grid.Resources>
    <Grid.RowDefinitions>
      <RowDefinition Height="30"></RowDefinition>
      <RowDefinition Height="300*"></RowDefinition>
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
      <Label x:Name="titleLabel" Style="{StaticResource titleStyle}" />
    </StackPanel>
    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
      <ComboBox Margin="0,0,4,0" x:Name="choicesList" Height="24" SelectionChanged="ListSelectionChanged" />
      <Rectangle VerticalAlignment="Stretch" Width="2" Margin="2" Stroke="#111111" />
      <ComboBox x:Name="selectedColumns" Tag="HealingBreakdownColumns" Height="26" Width="140" Margin="0,0,4,0" DropDownClosed="SelectDataGridColumns" BorderBrush="#FF707070">
        <ComboBox.ItemTemplateSelector>
          <local:ComboBoxItemTemplateSelector>
            <local:ComboBoxItemTemplateSelector.SelectedItemTemplates>
              <DataTemplate DataType="{x:Type local:ComboBoxItemDetails}">
                <TextBlock Text="{Binding SelectedText}" />
              </DataTemplate>
            </local:ComboBoxItemTemplateSelector.SelectedItemTemplates>
            <local:ComboBoxItemTemplateSelector.DropDownItemTemplates>
              <DataTemplate DataType="{x:Type local:ComboBoxItemDetails}">
                <StackPanel Orientation="Horizontal">
                  <CheckBox IsChecked="{Binding IsChecked}" Width="20" />
                  <TextBlock Text="{Binding Text}" IsHitTestVisible="false" />
                </StackPanel>
              </DataTemplate>
            </local:ComboBoxItemTemplateSelector.DropDownItemTemplates>
          </local:ComboBoxItemTemplateSelector>
        </ComboBox.ItemTemplateSelector>
      </ComboBox>
    </StackPanel>
    <syncfusion:SfDataGrid x:Name="dataGrid"
                           AllowDraggingColumns="True"
                           AllowResizingColumns="True"
                           AllowSorting="True"
                           AutoGenerateColumns="False"
                           ColumnSizer="Auto"
                           FrozenColumnCount="1"
                           Grid.Row="1"
                           GridCopyOption="CopyData,IncludeHeaders,IncludeFormat"
                           IsReadOnly="True"
                           SelectionMode="Extended"
                           ShowRowHeader="False">
      <syncfusion:SfDataGrid.Columns>
        <syncfusion:GridTextColumn MappingName="Name" HeaderText="Name" />
        <syncfusion:GridTextColumn MappingName="Precent" SortMode="Value" DisplayBinding="{Binding Path=Percent, Converter={StaticResource ZeroConverter}}" HeaderToolTipTemplate="{StaticResource HeaderTotalTemplateToolTip}" TextAlignment="Right" HeaderText="% Player" />
        <syncfusion:GridTextColumn MappingName="Total" SortMode="Value" DisplayBinding="{Binding Path=Total, Converter={StaticResource NumberFormatConverter}}" TextAlignment="Right" HeaderText="Heal" />
        <syncfusion:GridTextColumn MappingName="Extra" SortMode="Value" DisplayBinding="{Binding Path=Extra, Converter={StaticResource NumberFormatConverter}}" HeaderToolTipTemplate="{StaticResource HeaderOverHealTemplateToolTip}" TextAlignment="Right" HeaderText="Over Healed" />
        <syncfusion:GridTextColumn MappingName="DPS" SortMode="Value" DisplayBinding="{Binding Path=DPS, Converter={StaticResource NumberFormatConverter}}" HeaderToolTipTemplate="{StaticResource HeaderHPSTemplateToolTip}" TextAlignment="Right" HeaderText="HPS" />
        <syncfusion:GridTextColumn MappingName="SDPS" SortMode="Value" DisplayBinding="{Binding Path=SDPS, Converter={StaticResource NumberFormatConverter}}" HeaderToolTipTemplate="{StaticResource HeaderSHPSTemplateToolTip}" TextAlignment="Right" HeaderText="SHPS" />
        <syncfusion:GridTextColumn MappingName="TotalSeconds" SortMode="Value" DisplayBinding="{Binding Path=TotalSeconds, Converter={StaticResource NumberFormatConverter}}" TextAlignment="Right" HeaderText="Sec" />
        <syncfusion:GridTextColumn MappingName="Hits" SortMode="Value" DisplayBinding="{Binding Path=Hits, Converter={StaticResource NumberFormatConverter}}" TextAlignment="Right" HeaderText="# Heals" />
        <syncfusion:GridTextColumn MappingName="Max" SortMode="Value" DisplayBinding="{Binding Path=Max, Converter={StaticResource NumberFormatConverter}}" TextAlignment="Right" HeaderText="Max Heal" />
        <syncfusion:GridTextColumn MappingName="Avg" SortMode="Value" DisplayBinding="{Binding Path=Avg, Converter={StaticResource NumberFormatConverter}}" TextAlignment="Right" HeaderText="Av Heal" />
        <syncfusion:GridTextColumn MappingName="AvgCrit" SortMode="Value" DisplayBinding="{Binding Path=AvgCrit, Converter={StaticResource NumberFormatConverter}}" TextAlignment="Right" HeaderText="Av Crit" />
        <syncfusion:GridTextColumn MappingName="AvgLucky" SortMode="Value" DisplayBinding="{Binding Path=AvgLucky, Converter={StaticResource NumberFormatConverter}}" TextAlignment="Right" HeaderText="Av Lucky" />
        <syncfusion:GridTextColumn MappingName="ExtraRate" SortMode="Value" DisplayBinding="{Binding Path=ExtraRate, Converter={StaticResource ZeroConverter}}" HeaderToolTipTemplate="{StaticResource HeaderOverHealPercentTemplateToolTip}" TextAlignment="Right" HeaderText="% Over" />
        <syncfusion:GridTextColumn MappingName="CritRate" SortMode="Value" DisplayBinding="{Binding Path=CritRate, Converter={StaticResource ZeroConverter}}" TextAlignment="Right" HeaderText="% Crit" />
        <syncfusion:GridTextColumn MappingName="LuckRate" SortMode="Value" DisplayBinding="{Binding Path=LuckRate, Converter={StaticResource ZeroConverter}}" TextAlignment="Right" HeaderText="% Luck" />
        <syncfusion:GridTextColumn MappingName="TwincastRate" SortMode="Value" DisplayBinding="{Binding Path=TwincastRate, Converter={StaticResource ZeroConverter}}" TextAlignment="Right" HeaderText="% TC" />
      </syncfusion:SfDataGrid.Columns>
    </syncfusion:SfDataGrid>
  </Grid>
</local:BreakdownTable>

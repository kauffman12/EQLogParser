<syncfusion:ChromelessWindow x:Class="EQLogParser.TimelineLayoutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:fa="http://schemas.fontawesome.com/icons/"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        TitleBarHeight="{DynamicResource EQTableHeaderRowHeight}"
        TitleFontSize="{DynamicResource EQContentSize}"
        TitleBarForeground="{DynamicResource PrimaryDarken}"
        Icon="EQLogParser.ico"
        TitleTextAlignment="Left"
        WindowStyle="ToolWindow"
        Loaded="WindowLoaded"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        MinWidth="400"
        Title="Save Timeline Layout">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    
    <!-- TextBox input (no label to save space) -->
    <TextBox x:Name="layoutNameTextBox" 
             Grid.Row="0"
             Margin="20,20,20,10"
             Padding="4"
             FontSize="{DynamicResource EQContentSize}"
             BorderBrush="{DynamicResource PrimaryDarken}"
             BorderThickness="1"
             Background="{DynamicResource ContentBackground}"
             Foreground="{DynamicResource ContentForeground}" 
             TextChanged="layoutNameTextBox_TextChanged"
             KeyDown="layoutNameTextBox_KeyDown" />
    
    <!-- Error message (hidden by default) -->
    <TextBlock x:Name="errorMessage" 
               Grid.Row="1"
               Visibility="Collapsed"
               Margin="20,0,20,10"
               Foreground="{DynamicResource EQWarnForegroundBrush}"
               FontSize="{DynamicResource EQContentSize}" />
    
    <!-- Buttons -->
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,20">
      <Button Content="Save" 
              x:Name="saveButton" 
              IsDefault="True"
              Height="{DynamicResource EQButtonHeight}"
              MinWidth="80"
              Padding="15,0,15,0"
              Margin="0,0,10,0"
              Foreground="{DynamicResource PrimaryDarken}"
              Click="SaveButtonClick" />
      <Button Content="Cancel" 
              x:Name="cancelButton" 
              Height="{DynamicResource EQButtonHeight}"
              MinWidth="80"
              Padding="15,0,15,0"
              Foreground="{DynamicResource PrimaryDarken}"
              Click="CancelButtonClick" />
    </StackPanel>
  </Grid>
</syncfusion:ChromelessWindow>
